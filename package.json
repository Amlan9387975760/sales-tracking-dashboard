{
  "name": "sales-dashboard",
  "version": "2.0.0",
  "description": "Sales Lifecycle Intelligence Dashboard — Google Sheets backend",
  "scripts": {
    "dev": "cd frontend-astro && npm run dev",
    "build": "cd frontend-astro && npx astro build",
    "build:github": "cd frontend-astro && BASE=/sales-tracking-dashboard/ npx astro build && cp -r dist/* ../docs/"
  }
}
